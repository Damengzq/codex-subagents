interface:
  display_name: "工作进度"
  short_description: "收集安排+扫描待办，生成结构化日程早报"
  default_prompt: "Use daily-briefing to collect today's schedule, scan hardware-wiki TODOs, and generate a structured morning briefing."
policy:
  allow_implicit_invocation: true
